# Позор-дом

Позор-дом - это система умного дома с распределенной архитектурой для обмена сообщениями между клиентами через WebSocket соединения. Система обеспечивает полную видимость всех сообщений в сети через центральный хаб.

## Архитектура

### Позор-дом Cloud

Облачный релейный сервер для маршрутизации сообщений между клиентами и хабами. Обеспечивает подключение клиентов из внешних сетей к локальным хабам.

**Порт:** 8081 (ws://localhost:8081)
**Функции:**

- Прием подключений от клиентов и хабов
- Широковещательная рассылка сообщений всем подключенным клиентам
- Логирование всей активности

### Позор-дом Hub

Центральный хаб системы - одновременно локальный сервер и клиент облачного релея:

- **Локальный сервер** (порт 8082): Принимает подключения от клиентов в локальной сети
- **Облачный клиент**: Подключается к Cloud для ретрансляции сообщений
- **Центр мониторинга**: Видит ВСЕ сообщения в системе (от локальных клиентов + через Cloud)

Хаб транслирует сообщения от терминала ко всем клиентам и логирует всю активность сети.

**Порт:** 8082 (ws://localhost:8082)

### Позор-дом Client

Клиентское приложение для подключения к серверам Cloud или Hub. Позволяет отправлять и получать сообщения в интерактивном режиме через терминал.

### Позор-дом Shared

Общая библиотека с утилитами, константами и общими функциями, используемыми всеми компонентами системы. Обеспечивает:

- Единообразие протоколов сообщений
- Централизованную конфигурацию
- Общие утилиты логирования и обработки сообщений

## Установка

### Требования

- Rust 1.70 или выше
- Cargo (поставляется с Rust)

### Сборка проекта

```bash
# Клонирование репозитория
git clone https://github.com/smileyfaceskobochka/pozor-dom.git
cd pozor-dom

# Сборка всех компонентов
cargo build --release

# Или сборка отдельных компонентов
cargo build --release --bin pozor-dom-cloud
cargo build --release --bin pozor-dom-hub
cargo build --release --bin pozor-dom-client
```

## Запуск

### 1. Запуск Cloud (туннель)

```bash
cargo run --bin pozor-dom-cloud
```

Cloud автоматически запускается на порту 8081 (или на порту из переменной окружения `POZOR_DOM_CLOUD_PORT`).

### 2. Запуск Hub (локальный сервер)

```bash
# Подключение к локальному Cloud (по умолчанию 127.0.0.1:8081)
cargo run --bin pozor-dom-hub

# Или подключение к удаленному Cloud серверу
cargo run --bin pozor-dom-hub your-cloud-server.com
```

Hub запускается на порту 8082 и подключается к Cloud для ретрансляции сообщений.

### 3. Запуск Client (для тестирования)

```bash
# Подключение к Cloud
cargo run --bin pozor-dom-client ws://localhost:8081

# Или подключение к Hub (если в той же сети)
cargo run --bin pozor-dom-client ws://localhost:8082
```

### Конфигурация через переменные окружения

```bash
# Cloud configuration
export POZOR_DOM_CLOUD_HOST="your-public-ip"
export POZOR_DOM_CLOUD_PORT="8081"

# Hub configuration
export POZOR_DOM_HUB_HOST="127.0.0.1"
export POZOR_DOM_HUB_PORT="8082"

# Then run components
cargo run --bin pozor-dom-cloud
cargo run --bin pozor-dom-hub
```

Все компоненты поддерживают интерактивный ввод через терминал. Введите сообщения и нажмите Enter для отправки.

## Возможности

### ✅ Текущие возможности

- **Распределенная архитектура**: Cloud + Hub + Clients
- **Полная видимость**: Hub видит все сообщения в системе
- **Интерактивное общение**: Терминальный ввод/вывод для всех компонентов
- **Гибкая конфигурация**: Переменные окружения для настройки
- **Общая библиотека**: Единообразие протоколов и утилит
- **Логирование активности**: Мониторинг всех подключений и сообщений

### 🎯 Примеры использования

1. **Удаленное управление**: Клиент подключается к Cloud, Hub получает команды
2. **Мониторинг сети**: Hub логирует всю активность системы
3. **Локальная сеть**: Клиенты подключаются напрямую к Hub
4. **Гибридная конфигурация**: Одновременная работа локальных и удаленных клиентов

## Разработка

### Структура проекта

```bash
pozor-dom/
├── pozor-dom-cloud/     # Облачный туннель
├── pozor-dom-hub/       # Центральный хаб
├── pozor-dom-client/    # Клиентское приложение
├── pozor-dom-shared/    # Общие утилиты
├── Cargo.toml           # Конфигурация workspace
└── README.md
```

## Безопасность

**Текущее состояние:** Система использует обычные WebSocket соединения (ws://) без шифрования.

**Для продакшена необходимо добавить:**

- TLS шифрование (WSS)
- Аутентификацию клиентов
- Авторизацию доступа к хабу

**Текущие меры безопасности:**

- Cloud компонент не хранит данные локально
- Доступ к хабу через авторизованный туннель
- Логирование всей активности для мониторинга

## Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Создайте Pull Request

## Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## Контакты

- Репозиторий: <https://github.com/smileyfaceskobochka/pozor-dom>
- Issues: <https://github.com/smileyfaceskobochka/pozor-dom/issues>
